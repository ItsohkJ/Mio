<!DOCTYPE html>
<html>
  <head>
    <title>miu</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" charset='UTF-8' />
    <link rel="stylesheet" href="/style.css" />
    <style>
      body {
        background: #e6e6fa;
        height: 100vh;
        width: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        margin: 0;
        padding: 0;
        text-align: center;
        font-family: Monospace;
        flex-direction: column;
      }
      
      .container{
        background: #cbc3e3;
        border-radius: 50px;
        width: 16rem;
        margin-bottom: 10px;
      }
      input {
        width: 12em;
        border-radius: 5px;
        padding: 5px;
        border: solid 1px purple;
        margin: 10px auto;
      }
      .regForm {
        padding: 2rem 2rem 1rem 2rem;
      }
      a {
        color: purple;
        text-decoration: none;
      }
      
            
      .btn {
        margin: 10px;
        padding: 8px;
        background-color: rgba(210, 145, 255, 0.5);
        border-radius: 5px;
        border: none;
        
        }
          .error {
            margin-bottom: 5px;
          }  
            
            
    </style>
  </head>
  <body>
    
    <h1>Welcome Back to Mio!</h1>
          <% if(messages.error) {%>
          <div class="error">
            <%=messages.error%>
          </div>
        <%}%>
    <div>
    <div class="container">

      <div class="regForm">
        <form  action="/login" method="post">
          <label for="email">Your Email:</label><br />
          <input type="email" name="loginEmail" /><br />
          <label for="password">Your Password:</label><br />
          <input type="password" name="loginPassword" /><br />
          <button class="btn">Sign In</button>
        </form>
        
      </div>
    </div>
    New to miu? <a href="/register">Sign Up!</a>
    </div>
    
  </body>
</html>